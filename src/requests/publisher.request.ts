export interface PublisherCreateRequest {
    name: string;
    address: string;
    phone: string;
}

export interface PublisherUpdateRequest {
    name: string;
    address: string;
    phone: string;
}
